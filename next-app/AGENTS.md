# AGENTS.md — next-app

Next.js App Router app with server actions, React Query, and Vitest testing.

## Before pushing, always run:

```bash
pnpm lint && pnpm build && pnpm test:run
```

All three must pass with zero errors before you commit and push. A broken build on main means a broken deployment.
