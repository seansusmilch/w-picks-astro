---
const { pb, apb, user } = Astro.locals;

if (Astro.request.method === 'POST') {
  // Create or update pick

  const formData = await Astro.request.formData();
  console.log('formdata', formData);
  return Astro.redirect('/matchups');
}
---
