---
// This page is a route to have a dynamic redirect for the PWA
const { isAuthed } = Astro.locals;

let redirect = '/stats/weekly';

if (!isAuthed) {
  redirect = '/';
}

return Astro.redirect(redirect);
---
